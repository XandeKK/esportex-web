
<%= render partial: "shared/modal_logout" %>

<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="d-flex container">
        <%= link_to "Home", root_path, class:"navbar-brand" %>

                <%= link_to "Map", map_games_path, class:"nav-link", data: { turbo: false } %>
        <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
            </li>
        </ul>
        
        <% if Current.user %>
          <div class="dropdown">
            <a class="nav-link dropdown-toggle bg-light rounded-circle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false">
              <i class="fa-solid fa-user"></i>
            </a>
            <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
              <li>
                <%= link_to "Your Profile", profile_path(Current.user[:username]), class: "dropdown-item"%>
              </li>
              <li>
                <%= link_to "Edit profile", edit_profile_path, class: "dropdown-item"%>
              </li>
              <li><hr class="dropdown-divider"></li>
              <li>
                
                <button type="button" class="dropdown-item" data-bs-toggle="modal" data-bs-target="#exampleModal">
                  Logout
                </button>
              </li>
            </ul>
          </div>
        <% end %>
    </div>
</nav>
